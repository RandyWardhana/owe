# Supabase — optional. Without these, owe runs fully offline (localStorage only)
# and shared "paid" state is not synced across devices.
# Find them in your Supabase project: Settings → API.
NEXT_PUBLIC_SUPABASE_URL=https://example-supabase.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=ey....
